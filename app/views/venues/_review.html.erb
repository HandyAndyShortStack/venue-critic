<% if review.note then quote = review.note else quote = review.dispute end %>
<p>
  <% if review.overall then "★" * review.overall else "no ratings" end %>
  <br />
  <% if quote %>
    "<%= quote[0..50] %><%= "..." if quote.length > 50 %>"
  <% end %>
  <br />
  <%= link_to "read more", venue_review_path(review) %>
</p>

